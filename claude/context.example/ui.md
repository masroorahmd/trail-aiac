# UI — agent context

> Read by: UI Developer (primary), Test Manager, Technical Writer.
> Maintained by: UI Developer.
>
> Purpose: UI conventions and the design system in use. Agents reference
> this when building or reviewing user-facing screens.

## Design system
<!-- Component library, design tokens, where the source of truth lives. -->

## Layout & spacing
<!-- Grid, breakpoints, spacing scale. -->

## Typography & colour
<!-- Type scale, palette, contrast policy. -->

## Accessibility baseline
<!-- WCAG level targeted, keyboard nav rules, ARIA conventions. -->

## Interaction patterns
<!-- Forms, modals, navigation patterns used consistently. -->

## What to avoid
<!-- Patterns deliberately rejected. -->

## Design records
<!-- Per-feature design sessions live in `design/<slug>/` at the repo
     root, not here: mock HTML plus a DESIGN.md carrying the screen ×
     state matrix and numbered `D-N` decisions (the `/mock` lane, see
     the `ui-mockup` skill). This file holds only what outlives a
     single feature — a session's *Rules for ui.md* section is the
     shortlist UD copies up. List the folders here so the next session
     can find what was already decided. -->
