# Marketing Manager — Memory

> Persistent notes carried across sessions. Loose convention: the
> sections below are suggestions. Entries should be concrete and
> dated (YYYY-MM-DD). When a section grows past ~10 lines, spill
> detail into a sibling file (`brand-decisions-YYYY.md`,
> `seo-log-YYYY.md`, `audience-log-YYYY.md`) and keep this file as
> the index.

## Decisions
<!-- Framing or positioning calls made together with USER, with a
     one-line rationale. E.g. "scoped .com hero to a single demo
     CTA; rejected carousel because it dilutes the conversion
     goal". Audience-track decisions belong here too — when USER
     said "this story is OSS, not EE" and the reasoning is non-
     obvious. -->

## Audience-message pairings
<!-- What worked, what didn't, after a page shipped. E.g.
     "evaluator persona on .org responds to operational pain stories;
     ignores feature lists" — log the lesson, dated, with the page
     and the signal that taught it. -->

## Brand-voice anchors
<!-- Concrete phrasings that have become part of the voice — sentences,
     metaphors, technical claims worded a specific way that USER
     approved. Helps stay consistent across pages and posts. Counter-
     examples (rejected phrasings + reason) are equally valuable. -->

## SEO learnings
<!-- Keyword clusters that converted vs. that did not. Competitor
     moves worth noting. Search-intent surprises (a keyword that
     looked transactional turned out informational). -->

## Cross-agent handovers
<!-- Append one line per handover. Don't duplicate the DoD comment
     here — just enough to find the work-item later. -->

## Lessons learned
<!-- What worked, what didn't, what to do differently next time —
     specifically when an escalation, a re-scoping, or a USER
     correction has shifted how to scope similar Stories. -->

## Open threads
<!-- What you're waiting on — a decision from USER, a finished
     component from ui-developer, a docs page from tech-writer, a
     DNS cutover from release-manager. Ticket IDs only; details live
     on the work-item. -->
